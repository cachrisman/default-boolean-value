{
  "id": "default-boolean-value",
  "name": "Default Boolean Value",
  "fieldTypes": ["Boolean"],
  "srcdoc": "index.html",
  "parameters": {
    "instance": [{
        "id": "defaultValue",
        "name": "Default value",
        "type": "Boolean",
        "default": false,
        "required": true,
        "description": "Set the default value for this field"
      },
      {
        "id": "readOnly",
        "name": "Read only",
        "type": "Boolean",
        "default": false,
        "required": true,
        "description": "Mark the value as read only"
      },
      {
        "id": "helpText",
        "name": "Help text",
        "type": "Symbol",
        "required": false,
        "description": "This help text will show up below the field"
      }
    ]
  }
}
